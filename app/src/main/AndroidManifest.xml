<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.jonas.edu.meugerenciadorfinanceiro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".CategoriaActivity"
            android:label="@string/title_activity_categoria"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SituacaoActivity" />
        <activity android:name=".TipoActivity" />
        <activity android:name=".TotalActivity" />
        <activity android:name=".ConsultarActivity" />
        <activity
            android:name=".ParcelasActivity"
            android:label="@string/title_activity_parcelas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".InicioActivity"
            android:label="@string/title_activity_inicio"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".contas.CriarContaActivity" />
        <activity android:name=".contas.ListarContaActivity" />
        <activity android:name=".contas.EditarContaActivity" />
        <activity android:name=".contas.ConsultarContaActivity" />
        <activity android:name=".BuscaConta"></activity>
    </application>

</manifest>